interface:
  display_name: "Implementation Detail Timing"
  short_description: "判断实现细节命名时机，并选择层级依赖校验工具"
  default_prompt: "Use $implementation-detail-timing to decide which implementation details may be fixed now and which architecture or layer dependency checks must be enforced."
